<div class="form-group">
    <label for="{{$name}}">Password</label>
    <input
      type="password"
      class="form-control"
      id="{{$name}}"
      name="{{$name}}"
      value="{{$value}}"
      placeholder="{{$placeholder}}"
    />
  </div>